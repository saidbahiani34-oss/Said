# 🚀 كيفية تشغيل البوت 24/7 (دون توقف)

لجعل الإشارات تعمل بشكل آلي ومستمر حتى لو أغلقت هاتفك أو حاسوبك، يجب رفع هذا الكود على **خادم سحابي (Cloud Server)**.

إليك أسهل الطرق المجانية أو الرخيصة للقيام بذلك:

## الخيار 1: النشر على Render (الأسهل)

1. **أنشئ حساباً على GitHub** وارفع هذا الكود عليه.
2. اذهب إلى موقع [Render.com](https://render.com) وأنشئ حساباً.
3. اضغط على **New +** ثم اختر **Web Service**.
4. اربط حساب GitHub واختر المستودع (Repository) الذي رفعت عليه الكود.
5. في الإعدادات:
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `npx tsx server.ts`
6. اضغط **Create Web Service**.
7. سيعمل البوت الآن 24/7 ويرسل الإشارات إلى تلغرام.

## الخيار 2: النشر على Railway

1. اذهب إلى [Railway.app](https://railway.app).
2. سجل الدخول بواسطة GitHub.
3. اضغط **New Project** -> **Deploy from GitHub repo**.
4. اختر المستودع الخاص بك.
5. سيقوم Railway باكتشاف الإعدادات تلقائياً وتشغيل البوت.

## الخيار 3: خادم VPS (للمحترفين)

إذا كان لديك خادم Ubuntu (مثل DigitalOcean أو Hetzner):

1. ثبت Node.js:
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
   sudo apt-get install -y nodejs
   ```

2. ارفع الكود وشغله باستخدام PM2 (لضمان عدم توقفه):
   ```bash
   npm install -g pm2
   npm install
   npm run build
   pm2 start "npx tsx server.ts" --name "scalpmaster"
   pm2 save
   pm2 startup
   ```

---

### ⚠️ ملاحظة هامة
تأكد من وضع إعدادات تلغرام (Bot Token و Chat ID) في متغيرات البيئة (Environment Variables) في منصة الاستضافة لضمان الأمان، أو إدخالها عبر واجهة الموقع بعد التشغيل.
